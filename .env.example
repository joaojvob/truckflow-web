# API Laravel (Sail) — proxy Vite em dev aponta para http://localhost
VITE_API_BASE_URL=/api/v1

# Reverb — tracking em tempo real (mesmos valores do truckflow-api/.env)
VITE_REVERB_APP_KEY=truckflow-key
VITE_REVERB_HOST=localhost
VITE_REVERB_PORT=8080
VITE_REVERB_SCHEME=http
